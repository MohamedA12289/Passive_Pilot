Frontend 4: Marketing Pages Pack

Copy/merge the /app/* routes from this zip into your frontend project root.
These pages assume you already have:
- components/navigation.tsx
- components/footer.tsx
from Frontend 2/3 zips.

Then restart:
  npm run dev

Routes added:
 /, /pricing, /how-it-works, /free-training, /mentorship, /faq, /contact,
 /links, /privacy, /terms, /onboarding-guide
